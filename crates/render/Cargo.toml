[package]
name = "sketchmotion-render"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
sketchmotion-core = { path = "../core" }
# Mantido para composição vetorial, blending e efeitos das próximas fases
# (v0.2+). Na v0.1 a composição das camadas é feita em CPU (ver lib.rs).
skia-safe = "0.78"
