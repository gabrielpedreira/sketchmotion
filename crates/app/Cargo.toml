[package]
name = "sketchmotion"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
# Único crate que pode depender de todos os outros (camada de composição).
# As dependências entre módulos serão adicionadas a partir da Etapa 2.
