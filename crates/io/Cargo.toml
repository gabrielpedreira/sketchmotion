[package]
name = "sketchmotion-io"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
sketchmotion-core = { path = "../core" }
# Serialização binária compacta (CBOR) do formato de projeto.
ciborium = "0.2"
